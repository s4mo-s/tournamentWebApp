### IIS-Student-Tournament-frontend

- npm i (install dependencies)
- npm install -g @angular/cli (install the Angular CLI)
- ng serve (run the app for local environment)

App will run on http://localhost:4200/